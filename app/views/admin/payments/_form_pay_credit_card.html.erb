<% 
  pay_cc = PayCreditCard.new
  pay_cc.casher_id = current_employee.employee_info.account.id if current_employee.employee_info.account
%>
<%= form_for( [:admin, pay_cash], :remote => true) do |f| %>
  <%= hidden_field_tag :order_id, params[:order_id] %>
  <table class='compact'>
    <tr>
      <td>Pay:</td>
      <td><input type='text' id='recv_cash' value='0.00' class='large-amount' /></td>
      <td>Change:
        <label id='pay_cash_change' class='pay-amount'>0.00</label>
        <%= f.hidden_field :amount %>
      </td>
    </tr>
    <tr>
      <td>Cash received by:</td>
      <td><%= f.select('account_id', Account.where(:status => 1).where(:owner_type => 'EmployeeInfo').collect{|d| [d.account_name, d.id]}, {:include_blank => true }) %>  	
      </td>
      <td></td>
    </tr>
  </table>
  <p>
    <%= f.submit %>
  </p>
<% end %>


		<table style="width:auto;">
			<tr>
				<td>Full name:</td>
				<td><%= text_field_tag 'credit_card[full_name]' %></td>
			</tr>
			<tr>
				<td>Card type:</td>
				<td><%= select('credit_card','card_type', %w[Visa Master AE]) %></td>
			</tr>
			<tr>
				<td>Card Number:</td>
				<td><%= text_field_tag 'credit_card[card_number]' %></td>
			</tr>
			<tr>
				<td>Valid date:</td>
				<td><%= text_field_tag 'credit_card[valid_date]' %></td>
			</tr>
			<tr>
				<td>CSC:</td>
				<td><%= text_field_tag 'credit_card[csc]' %></td>
			</tr>
			<tr>
				<td>Amount:</td>
				<td><%= text_field_tag 'credit_card[amount]' %></td>
			</tr>
			<tr>
				<td>Prof code:</td>
				<td><%= text_field_tag 'credit_card[prof_code]' %></td>
			</tr>
			<tr>
				<td>Service fee:</td>
				<td><%= text_field_tag 'credit_card[service_fee]' %></td>
			</tr>
			<tr>
				<td>Total amount:</td>
				<td><%= text_field_tag 'credit_card[total_amount]' %></td>
			</tr>
		</table>
