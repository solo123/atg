  <tr id='<%= dom_id(row) %>'>
    <td><%= row.id %></td>
    <td><%= row.bill_type %></td>
    <td><%= row.bill_id %></td>
    <td><%= row.pay_before %></td>
    <td><%= row.amount %></td>
    <td><%= row.pay_after %></td>
    <td><%= row.created_at %></td>
    <td class='actions'>
        <%= link_to_edit_url edit_admin_payment_path(row), :title => "admin_edit_#{dom_id(row)}" %>
    </td>
  </tr>
