<% 
  pay_c = PayCompany.new
%>
<%= form_for( [:admin, pay_c], :remote => true) do |f| %>
  <%= hidden_field_tag :order_id, params[:order_id] %>
  <table class='compact'>
    <tr>
      <td>Amount:</td>
      <td><%= f.text_field :amount %></td>
    </tr>
    <tr>
      <td>Company order# </td>
      <td><%= f.text_field :company_order_number %></td>
    </tr>
  </table>
  <p>
    <%= f.submit %>
  </p>
<% end %>

