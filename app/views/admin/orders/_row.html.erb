    <tr>
        <td><%= link_to row.order_number, admin_order_path(row) %></td>
        <td><%= row.order_detail.full_name if row.order_detail %></td>
        <td><%= row.schedule.title if row.schedule %></td>
        <td><%= row.created_at %></td>
        <td><%= row.status %></td>
      <td class='actions'>
          <%= link_to_edit_url edit_admin_order_path(row), :title => "admin_edit_#{dom_id(row)}", :remote => true %>
      </td>
    </tr>
